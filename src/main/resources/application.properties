# =====================================================
# application.properties - COMPLETE CONFIGURATION
# =====================================================

# ===== Server Configuration =====
server.port=8080
server.servlet.context-path=/
spring.application.name=Activity Attendance System

# ===== Database Configuration =====
spring.datasource.url=jdbc:mysql://localhost:3306/face_attendance_activity?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ===== JWT Configuration =====
# ? QUAN TR?NG: PH?I CÓ 3 PROPERTIES NÀY
jwt.secret=XPRNYHjfEUze+lRbs0K/a+BRnfsC5ES6
jwt.expiration=86400000
jwt.refresh-token.expiration=604800000

# JWT Secret ph?i là Base64 string có ít nh?t 256 bits (32 bytes)
# ?? generate secret m?i:
# 1. Online: https://generate-random.org/api-key-generator
# 2. Command line: openssl rand -base64 32
# 3. Java: Keys.secretKeyFor(SignatureAlgorithm.HS256).getEncoded() -> Base64

# ===== QR Code Configuration =====
qr.code.storage.path=uploads/qrcodes
qr.code.width=300
qr.code.height=300

# ===== File Upload =====
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.location=uploads/temp

# ===== Swagger/OpenAPI =====
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha

# ===== Logging =====
logging.level.root=INFO
logging.level.com.tathanhloc.faceattendance=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.file.name=logs/activity-attendance.log
logging.file.max-size=10MB
logging.file.max-history=30

# ===== Jackson Configuration =====
spring.jackson.time-zone=Asia/Ho_Chi_Minh
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.serialization.write-dates-as-timestamps=false

# ===== Actuator (Health Check) =====
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ===== Email Configuration (Optional) =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true